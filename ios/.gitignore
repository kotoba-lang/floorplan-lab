# Generated by `xcodegen generate` from project.yml -- not checked in
# (README.md "ios/" section: `cd ios && xcodegen generate` regenerates
# this locally; hand-editing a checked-in .xcodeproj would fight the
# generator, same "generated, don't hand-edit" principle as
# manifest/west.yml elsewhere in this monorepo).
*.xcodeproj/
DerivedData/
*.xcuserstate
xcuserdata/
